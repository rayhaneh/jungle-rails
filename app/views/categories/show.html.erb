<section class="categories-show">

  <header class="page-header">
    <h1>
      <%= @category.name %>
      <small>(<%= pluralize @categories.size, 'category' %>)</small>
    </h1>
  </header>

  <div class="products">
    <%= render @category %>
  </div>

</section>
